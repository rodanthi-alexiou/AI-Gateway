<policies>
    <inbound>
        <base />
        <!-- Acquire a managed-identity token for Cognitive Services -->
        <authentication-managed-identity resource="https://cognitiveservices.azure.com"
                                         output-token-variable-name="managed-id-access-token"
                                         ignore-error="false" />
        <set-header name="Authorization" exists-action="override">
            <value>@("Bearer " + (string)context.Variables["managed-id-access-token"])</value>
        </set-header>
        <!-- Route to the priority-based OpenAI backend pool -->
        <set-backend-service backend-id="{backend-id}" />
    </inbound>
    <backend>
        <!-- Retry on 429 (throttled) or 503 (circuit open / unavailable).
             APIM will transparently select the next available backend in the pool. -->
        <retry count="3"
               interval="0"
               first-fast-retry="true"
               condition="@(context.Response.StatusCode == 429 || context.Response.StatusCode == 503)">
            <forward-request buffer-request-body="true" />
        </retry>
    </backend>
    <outbound>
        <base />
        <!-- Emit Azure OpenAI token metrics to Application Insights -->
        <!-- <azure-openai-emit-token-metric namespace="AIGateway">
            <dimension name="API ID" />
            <dimension name="Subscription ID" />
        </azure-openai-emit-token-metric> -->
    </outbound>
    <on-error>
        <base />
        <!-- Surface a clean error if every backend in the pool is unavailable -->
        <choose>
            <when condition="@(context.Response.StatusCode == 503)">
                <return-response>
                    <set-status code="503" reason="Service Unavailable" />
                    <set-body>{"error":{"code":"503","message":"All OpenAI backends are currently unavailable. Please retry later."}}</set-body>
                </return-response>
            </when>
        </choose>
    </on-error>
</policies>
